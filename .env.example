# Telegram
TELEGRAM_BOT_TOKEN=

# If set, the bot will use webhook mode instead of long polling.
# Example: https://bot.yourcompany.com/telegram/webhook
TELEGRAM_WEBHOOK_URL=

# Local port to listen on for webhook updates (only used when TELEGRAM_WEBHOOK_URL is set)
TELEGRAM_WEBHOOK_PORT=8080

# Public base URL of link portal (used in /link_planka message)

# Base URL of link portal (publicly reachable if users will link remotely)
# Example: https://bot.yourcompany.com
LINK_PORTAL_BASE_URL=http://localhost:8787

# Next.js admin panel auth (HTTP Basic)
ADMIN_BASIC_AUTH_USER=
ADMIN_BASIC_AUTH_PASS=

# 32-byte key, base64 encoded. Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
TOKEN_ENCRYPTION_KEY=

# SQLite database file
DATABASE_PATH=./data/dev.sqlite

# Optional Prisma DATABASE_URL. If not set, code will derive it from DATABASE_PATH.
DATABASE_URL=
