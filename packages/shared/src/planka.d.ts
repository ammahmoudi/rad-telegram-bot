/**
 * Validate if a Planka token is working by making a test API call
 * Returns true if token is valid, false otherwise
 */
export declare function validatePlankaToken(telegramUserId: string): Promise<boolean>;
